name: 'Pull Request'
on:
  pull_request:
    types: [opened, edited, synchronize, reopened, ready_for_review]

permissions:
  actions: read    # run checkout and reusable actions
  contents: read   # checkout
  packages: read   # install deps from GitHub Packages (ci-setup, ci-install)
jobs:
  pr-validate:
    uses: Inthememory/project-config/.github/workflows/pr.yml@ea22dde0ee9a3d32686f7927b6d3bfd745551dbb # main
    secrets: inherit
